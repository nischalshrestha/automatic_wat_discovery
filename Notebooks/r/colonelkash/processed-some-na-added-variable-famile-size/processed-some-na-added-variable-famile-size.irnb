{"cells":[{"metadata":{"_uuid":"2c560257ef101565a60b75381737756c808f68eb","_execution_state":"idle","trusted":true},"cell_type":"code","source":"# This R environment comes with all of CRAN preinstalled, as well as many other helpful packages\n# The environment is defined by the kaggle/rstats docker image: https://github.com/kaggle/docker-rstats\n# For example, here's several helpful packages to load in \n\nlibrary(ggplot2) # Data visualization\nlibrary(readr) # CSV file I/O, \nlibrary(rpart)\nlibrary(rpart.plot)\nlibrary(RColorBrewer)\n\nTrain <- read_csv(\"../input/train.csv\")\nTrain\nTest <- read_csv(\"../input/test.csv\")\nstr(Train)\n\n#Processing Age\nmedian(Train$Age, na.rm= TRUE)\nTrain$Age[is.na(Train$Age)] <- median(Train$Age, na.rm = TRUE)\nis.na(Train$Age)\n\n#processing Embarkment\ntable(is.na(Train$Embarked))\naggregate(Survived ~ Embarked, Train , FUN = sum)\nTrain$Embarked[is.na(Train$Embarked)] <- \"S\"\n\n#processing a family\nTrain$Family_Size <- Train$SibSp + Train$Parch +1\nTrain$Family_Size\nTest$Family_Size <- Test$SibSp + Test$Parch +1\n\n\nSurvived_Data <- rpart( Survived ~ Sex + Pclass + SibSp + Parch + Fare + Embarked + Family_Size , Train , method = \"class\")\n\nplot(Survived_Data, uniform=TRUE)\ntext(Survived_Data, cex = .8)\n\nPrediction <- predict(Survived_Data , Test , type = \"class\")\nTitanic_Result_2 <- data.frame(PassengerId = Test$PassengerId , Survived = Prediction , row.names = NULL)\nTitanic_Result_2\nwrite.csv(Titanic_Result_2, file = \"Titanic_Result_2.csv\", row.names = FALSE)\n\n\n# Input data files are available in the \"../input/\" directory.\n# For example, running this (by clicking run or pressing Shift+Enter) will list the files in the input directory\n\n\n# Any results you write to the current directory are saved as output.","execution_count":null,"outputs":[]}],"metadata":{"kernelspec":{"display_name":"R","language":"R","name":"ir"},"language_info":{"mimetype":"text/x-r-source","name":"R","pygments_lexer":"r","version":"3.4.2","file_extension":".r","codemirror_mode":"r"}},"nbformat":4,"nbformat_minor":1}